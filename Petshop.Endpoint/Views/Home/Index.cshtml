@model ProductIndexViewModel
<div class="content">

    <div class="row">
       

    <ul>
        @foreach (var item in Model.Data.Data)
        {

                @Html.Partial("PartialIndexsummery",item);
        }
      
    </ul>
    </div>
    
    <div class="btn-group m-1" page-model="@Model.Data.PageInfo" page-action="Index"
         page-classes-enable="true" page-class="btn "
    page-class-normal="btn-outline-dark" page-class-selected="btn-info">

    </div>
</div>
